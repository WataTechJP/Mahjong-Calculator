---
name: codex-reviewer
description: A review gate that uses Codex CLI (read-only) to iteratively perform: Review → Claude Code fixes → Re-review, until ok: true is reached. This process is executed after Claude Code completes implementation, file modifications, and implementation plan tasks.
---

詳細な手順は `INSTRUCTIONS.md` を参照（このディレクトリ内）。
